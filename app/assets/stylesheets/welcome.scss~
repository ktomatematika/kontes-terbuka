// Place all the styles related to the welcome controller here.
// They will automatically be included in application.css.
// You can use Sass (SCSS) here: http://sass-lang.com/
@import "bootstrap-sprockets";
@import "bootstrap";

html, body {
  height: 100%;
  color: #222222;
}

header.teaser { 
  text-align: center;
  background: url(welcome-bg.jpg) center / cover;
  min-height: 100%;

  .container {
    padding: 50px; 
    margin: 0 auto;
  }

  .welcome-logo {
    padding: 10px;
    text-align: left;
  }

  h1 {
    font-size: 60px;
    margin: 50px 0;
  }

  h4 {
    margin: 30px 0;
  }
}

section {
  padding: 50px 0;
}

#features {
  background: #c8ee1b;

  div div {
    text-align: center;
  }

  .glyphicon {
    font-size: 40px;
    padding: 10px 0;
  }
}

#testimonies {
  background: #ee99cc;

  h1 {
    padding-bottom: 20px;
  }

  blockquote:nth-child(odd) {
    @extend .blockquote-reverse;
  }

  blockquote {
    margin: 5px 30px;
  }

  blockquote:first-child {
    margin-top: 0;
  }

  blockquote:last-child {
    margin-bottom: 0;
  }
}

#teaser {
  background: #8bfac0;
}
