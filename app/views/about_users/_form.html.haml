.form-group
  = f.label :name, 'Nama:'
.form-group
  = @about_user.nil? ? @user.fullname : @about_user.name 
.form-group
  = f.hidden_field :name, value: @about_user.nil? ? @user.fullname : @about_user.name
.form-group
  = f.label :description, 'Deskripsi:'
.form-group
  = f.text_area :description
.form-group
  = f.label :is_alumni, 'Alumni?'
.form-group
  = f.check_box :is_alumni
.form-group
  = f.label :image, 'Foto'
.form-group
  = f.file_field :image
